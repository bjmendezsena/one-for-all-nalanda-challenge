export interface ClientOptions {
	baseUrl: string;
	apiKey: string;
}

// TODO: add the request/response types mirroring the backend contract as each endpoint is implemented
// (docs/sdk/code_rules.md § 4, docs/business-rules.md).
export interface ValidationClient {}
