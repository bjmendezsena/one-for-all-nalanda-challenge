import type { ClientOptions, ValidationClient } from "./types";

// TODO: add the public methods listed in docs/sdk/architecture.md § 4 as each one is implemented.
export function createClient(options: ClientOptions): ValidationClient {
	void options;
	return {};
}
