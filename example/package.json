{
  "name": "@nalanda/validation-sdk-example",
  "version": "0.1.0",
  "private": true,
  "description": "Runnable integration example: consumes @nalanda/validation-sdk as an external consumer and asserts every documented behavior against a live backend.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nalanda/validation-sdk": "file:../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
